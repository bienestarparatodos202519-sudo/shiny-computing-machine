import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.agendaclinica.calendar',
  appName: 'Agenda Clinica',
  webDir: 'dist',
};

export default config;
